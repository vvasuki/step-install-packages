name: install-universe-packages
version: 1.1.1
summary: install-packages step supporting ubuntu universe packages
tags:
  - apt-get
  - update
  - install
  - packages
properties:
- name: packages
  type: string
  required: true
- name: clear-cache
  type: string
  required: false
  default: "true"
